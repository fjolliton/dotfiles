;;; gnus-personal.el -- GNUS local settings, not intended to be commited
;;; Commentary:
;;; (no comment)
;;; Code:

(require 'gnus)
(require 'gnus-msg)

(setq gnus-secondary-select-methods
      '((nnimap "imap.isp"
                (nnimap-address "imap.example.org")
                (nnimap-server-port 993)
                (nnimap-stream ssl)
                (nnimap-authinfo-file "~/.authinfo.gpg"))))

(setq gnus-posting-styles nil)

;;; gnus-personal.el ends here
